/**
 * This package contains a separate abc extension that extends the tracematch extension abc.tm so that
 * it generates dependent advice directly from tracematches. 
 */
package ca.mcgill.sable.clara.tm;